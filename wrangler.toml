name = "otakudesu-api"
main = "src/index.js"
compatibility_date = "2024-01-01"

# Workers configuration
# This will use default workers.dev subdomain
workers_dev = true

# Limits
[limits]
cpu_ms = 50

# Environment variables (optional)
[vars]
NODE_ENV = "production"